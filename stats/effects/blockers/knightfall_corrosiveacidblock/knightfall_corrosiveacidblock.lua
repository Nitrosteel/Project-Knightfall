function init()
   effect.addStatModifierGroup({{stat = "poisonResistance", amount = 0.25}, {stat = "corrosiveacidImmunity", amount = 1}})

   script.setUpdateDelta(0)
end

function update(dt)

end

function uninit()
  
end
